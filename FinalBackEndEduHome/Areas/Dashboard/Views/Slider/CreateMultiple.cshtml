@model CreateMultpleSliderVM
@{
    ViewData["Title"] = "CreateMultiple";
}

@section Scripts{
    <partial name="_PartialView" />
    <script src="https://cdn.ckeditor.com/ckeditor5/30.0.0/classic/ckeditor.js"></script>
    <script>
        ClassicEditor
            .create(document.querySelector('#Description'))
            .catch(error => {
                console.error(error);
            });
    </script>
}
<div class="main-panel">
    <div class="content-wrapper">
        <div class="row">
            <div class="col-md-6 grid-margin stretch-card">
                <div class="card">
                    <div class="card-body">
                        <h4 class="card-title">Create new Slider</h4>

                        <form method="post" asp-controller="slider" asp-action="CreateMultiple" enctype="multipart/form-data" class="forms-sample">
                            <div class="form-group">
                                <label for="exampleInputUsername1">Title</label>
                                <textarea asp-for="Title" class="form-control" id="exampleInputUsername1" placeholder="Title"></textarea>
                                <span asp-validation-for="Title"></span>
                            </div>



                            <div class="form-group">
                                <label for="exampleInputEmail1">Description</label>
                                <span asp-validation-for="Desc"></span>

                                <textarea asp-for="Desc" class="form-control" id="Description" placeholder="description"></textarea>
                            </div>
                            <div class="form-group">
                                <label for="exampleInputUsername1">Image</label>
                                <input accept="image/*" asp-for="Images" class="form-control" id="exampleInputUsername1" placeholder="Image">
                                <span asp-validation-for="Images"></span>
                            </div>

                            <button type="submit" class="btn btn-primary me-2">Submit</button>
                            <button class="btn btn-light">Cancel</button>
                        </form>
                    </div>
                </div>
            </div>

        </div>
    </div>
</div>
